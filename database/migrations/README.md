Las migraciones SQL incrementales nuevas pueden colocarse aquí (convención `NNN_descripcion.sql`). El bootstrap inicial del proyecto usa [`../schema/schema.sql`](../schema/schema.sql).

Los scripts históricos del dominio anterior están en [`../migrations_legacy/`](../migrations_legacy/).
