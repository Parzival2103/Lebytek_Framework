# Seeds legacy — baseline 2026-06

Copia de referencia de los seeds `010`–`035` antes de consolidarlos en [`../schema/schema.sql`](../schema/schema.sql).

No los usa el instalador ni `scripts/seed.php` en el flujo actual.
